{
  "name": "platform-factory-plugin-typescript-example",
  "private": true,
  "version": "1.0.0",
  "description": "Reference platform-factory v1 language plugin, built on @platform-factory/plugin-sdk's TypeScript types.",
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x plugin.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^20.0.0"
  }
}
