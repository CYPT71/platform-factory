{
  "name": "platform-factory-typescript-example",
  "private": true,
  "version": "1.0.0",
  "description": "Reference secure-oci v1 language plugin, built on @secure-oci/plugin-sdk's TypeScript types.",
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x plugin.js"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^20.0.0"
  }
}
