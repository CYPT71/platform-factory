[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "secure-oci-plugin"
version = "1.0.0"
description = "SDK for out-of-process secure-oci language plugins: the versioned, length-prefixed JSON-RPC protocol plugins speak over stdin/stdout, and the plugin-side Server."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.urls]
Homepage = "https://github.com/CYPT71/secure-oci-base"

[tool.setuptools.packages.find]
include = ["secure_oci_plugin*"]
